services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    # Optional: create `.env` next to this file with API keys for demo-chat routes
    # env_file:
    #   - .env
